import firebase from '@firebase/app';
import 'firebase/auth';
import 'firebase/database';
import 'firebase/firestore';

const firebaseConfig = {
    apiKey: " Create a FIREBASE project ",
    authDomain: " Create a FIREBASE project ",
    databaseURL: " Create a FIREBASE project ",
    projectId: " Create a FIREBASE project ",
    storageBucket: " Create a FIREBASE project ",
    messagingSenderId: " Create a FIREBASE project ",
    appId: " Create a FIREBASE project ",
    measurementId: " Create a FIREBASE project "
  };
  firebase.initializeApp(firebaseConfig);
  
  
  export default firebase;
